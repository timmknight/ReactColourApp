import { renderComponent, expect } from '../../test-helper';
import ColorSlider from '../../../src/components/color_slider';

describe('Color Slider', () => {
    let component;
    beforeEach(() => {

    });
    it('', () => {

        // expect
    });

});
