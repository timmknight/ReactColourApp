import { renderComponent, expect } from '../../test-helper';
import Slider from '../../../src/components/slider';

describe('Slider', () => {
    let component;
    beforeEach(() => {

    });
    it('', () => {

        // expect
    });

});
