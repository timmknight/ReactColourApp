import { renderComponent, expect } from '../../test-helper';
import ColorIndex from '../../../src/components/color_index';

describe('Color Index', () => {
    let component;
    beforeEach(() => {

    });
    it('', () => {

        // expect
    });

});
