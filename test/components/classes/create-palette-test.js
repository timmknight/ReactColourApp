import { renderComponent, expect } from '../../test-helper';
import CreatePalette from '../../../src/components/create_palette';

describe('Create Palette', () => {
    let component;
    beforeEach(() => {

    });
    it('', () => {

        // expect
    });

});
