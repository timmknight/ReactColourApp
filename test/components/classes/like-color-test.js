import { renderComponent, expect } from '../../test-helper';
import LikeColor from '../../../src/components/like_color';

describe('Like Color', () => {
    let component;
    beforeEach(() => {

    });
    it('', () => {

        // expect
    });

});
