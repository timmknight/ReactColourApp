import React from 'react';

const About = () => {
  return (
    <div id="about">This site was built by Tim Knight as a project to learn React.js. It uses a NodeJS & ExpressJS backend API and React.js on the front end. The react side uses
    simple stateless functional components as well as components built using ES6 classes. The project is deliberatly simple and doesn't use a Redux architecture. The API has limited validation and is deliberatly simple.</div>
  )
}

export default About;
